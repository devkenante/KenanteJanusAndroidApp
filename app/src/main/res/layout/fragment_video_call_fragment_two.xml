<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/MMLayout"
    android:clickable="true"
    android:focusable="true"
    android:background="@color/transparent"
    tools:context=".fragments.VideoCallFragmentTwo">

    <include
        android:id="@+id/confCallOnlyPersonView"
        layout="@layout/conf_call_wait_layout"/>

    <GridLayout
        android:id="@+id/videoGridLayout"
        style="@style/MMLayout"/>

    <FrameLayout
        style="@style/MWLayout"
        android:layout_gravity="bottom"
        android:id="@+id/bottomChatFragment"/>

    <LinearLayout
        style="@style/WMLayout"
        android:layout_gravity="end">

        <androidx.recyclerview.widget.RecyclerView
            style="@style/MMLayout"
            android:id="@+id/enlargeUserRV"
            android:visibility="gone"/>

    </LinearLayout>

</FrameLayout>